interface Pages {
  name: string;
  link: string;
}

const pages: Pages[] = [
  { name: 'Home', link: '/' },
  { name: 'Create Quiz', link: '/create-quiz' },
  { name: 'Play Quiz', link: '/play-main' },
];

export default pages;
